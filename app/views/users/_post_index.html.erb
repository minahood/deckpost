    <% if microposts.any? %>
      
      <ul class="index mt-up-1">
        <%= render microposts %>
      </ul>
      <div class="page_nav">
        <%= paginate microposts,window: 2 %>
      </div>
    <% else %>
      <p class="border-top mt-up-1 p-2">各当する投稿はありません。</p>
    
    <% end %>